services:
  app:
    build:
      context: .
    container_name: ai-document-backend-prod
    environment:
      APP_ENV: production
    ports:
      - 3000:3000
    depends_on:
      - db
      - redis
    volumes:
      - ./config:/app/config
      - ./src:/app/src
    networks:
      - app_net

  db:
    image: postgres:15-alpine
    container_name: postgres-ai-document-prod
    restart: always
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_DB=${DB_NAME}_prod
      - POSTGRES_PASSWORD=${DB_PASS}
    ports:
      - 5432:5432
    volumes:
      - db_data:/var/lib/postgresql/data
    networks:
      - app_net

  redis:
    image: redis:alpine3.22
    container_name: redis-ai-document-prod
    environment:
      - REDIS_PASSWORD=${REDIS_PASS}
    ports:
      - 6379:6379
    volumes:
      - redis_data:/data
    command: redis-server --requirepass ${REDIS_PASS}
    networks:
      - app_net

  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant-ai-document-dev
    restart: unless-stopped
    ports:
      - 6333:6333 # HTTP API
      - 6334:6334 # gRPC API
    networks:
      - app_net
    volumes:
      - qdrant_data:/qdrant/storage

networks:
  app_net:

volumes:
  db_data:
  qdrant_data:
  redis_data:
